# Scene 8: The Academy System

Purpose: transforms emotion into confidence by showing that NIDUS preparation is a complete, connected system.

Public API: `AcademySystemScene`.

Placeholder asset: `experienceSceneAssets.academySystem`.

Extension points: final mission-table photography can replace the placeholder asset, and the pathway labels can later be sourced from approved content without changing the scene shell.
