# NDIE Provider Selection Guide

Routing modes:

- automatic routing
- manual override
- priority chains
- fallback chains
- cost-aware routing
- accuracy-aware routing
- health-aware routing

The selector must prefer providers that support the requested language, document type, formula needs, table needs, diagram needs, and question detection needs.

Manual override should be used only for controlled testing or enterprise support.
